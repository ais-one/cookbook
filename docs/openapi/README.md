# OpenAPI Docs

Generated OpenAPI YAML output is stored in this folder.

Use the repository scripts from the workspace root to regenerate or validate the merged specification:

```bash
npm run docs:generate
npm run docs:validate
```

For HTML output, run:

```bash
npm run docs:make-html
```
